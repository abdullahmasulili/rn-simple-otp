import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },
  header: {
    display: "flex",
    alignItems: "center",
    flexDirection: "column",
    gap: 10,
    marginVertical: 45,
    marginHorizontal: 15,
  },
});
